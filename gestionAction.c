#include "structures.h"
#include "gestionAction.h"
#include <stdlib.h>

void gestionAction(semaine_t * sem, char * ligne) {
}

int * listeMotif(semaine_t * sem, char * motif) {
    return(NULL);
}

void libererListeActions(listeAction_t * liste) {
}

void afficherActions(listeAction_t * liste) {
}